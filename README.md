
  # Multi-Screen Bookmark Organizer

  This is a code bundle for Multi-Screen Bookmark Organizer. The original project is available at https://www.figma.com/design/8G7CW9txOG5ifD08if565Y/Multi-Screen-Bookmark-Organizer.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  